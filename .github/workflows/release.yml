name: Update Version and Tag
on:
  push:
    branches:
      - master

permissions:
  contents: write
  actions: write

jobs:
  release:
    uses: peavers-warcraft/workflow-templates/.github/workflows/release-on-push.yml@master
    with:
      toc_file: PeaversRemembersYou.toc
      addon_name: PeaversRemembersYou
    secrets: inherit
